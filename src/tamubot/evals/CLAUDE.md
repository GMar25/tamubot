# tamubot.evals

**Probe** = single-query pipeline test (`run_probe --suite smoke` or `--query "..."`).
**Benchmark** = A/B comparison across golden sets (`make bench`, `make bench-ragas`).

Use `--help` on each command and `make help` for full flag reference.

